language: python
python: 3.6-dev
install:
  - pip install -r requirements.txt
  - pip install coveralls

# command to run tests
script:
  - pythom -m pytest --cov=app/tests/v1
after_success:
- coveralls
